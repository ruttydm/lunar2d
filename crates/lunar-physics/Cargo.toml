[package]
name = "lunar-physics"
version = "0.1.0"
edition = "2021"
description = "Physics engine for Lunar3D — gravity, thrust, collision, projectiles, orbital mechanics"

[dependencies]

[dev-dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
panic = "abort"
